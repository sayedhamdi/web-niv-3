{
  "name": "server-1",
  "version": "0.0.1",
  "description": "test npm",
  "main": "correction.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon correction.js",
    "affichi-hello": "echo hello",
    "db:delete": "rm -rf ./db/users.db",
    "db:create": "touch ./db/users.db"
  },
  "keywords": [
    "NODEJS"
  ],
  "author": "Sayed",
  "license": "MIT",
  "dependencies": {
    "nodemon": "^2.0.20"
  }
}
